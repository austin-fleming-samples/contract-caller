import { Container } from "../container/container";

export const Navbar = () => (
	<Container
		tag="header"
		classes="sticky top-0 right-0 left-0 pt-3 font-bold text-sm tracking-widest"
	>
		<span>Caller</span>
	</Container>
);
